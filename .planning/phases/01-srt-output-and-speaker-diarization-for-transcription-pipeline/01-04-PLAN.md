---
phase: 01-srt-output-and-speaker-diarization-for-transcription-pipeline
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - meeting_notes/cli/commands/doctor.py
  - meeting_notes/cli/commands/init.py
autonomous: true
requirements: [D-13, D-14, D-15]
gap_closure: true

must_haves:
  truths:
    - "meet doctor displays PyannoteCheck, HuggingFaceTokenCheck, and PyannoteModelCheck in output"
    - "meet init --update shows field [7] HuggingFace token in update menu"
    - "'meet doctor' reports pyannote.audio check as OK (not ERROR) when pyannote.audio is installed"
  artifacts:
    - path: "meeting_notes/cli/commands/doctor.py"
      provides: "Pyannote health checks registered in doctor command"
      contains: "PyannoteCheck"
    - path: "meeting_notes/cli/commands/init.py"
      provides: "--update flag on init command"
      contains: "@click.option('--update'"
  key_links:
    - from: "meeting_notes/cli/commands/doctor.py"
      to: "meeting_notes/services/checks.py"
      via: "import and suite.register()"
      pattern: "suite\\.register\\(PyannoteCheck\\(\\)\\)"
    - from: "meeting_notes/cli/commands/init.py"
      to: "_update_specific_fields"
      via: "early exit when update=True"
      pattern: "if update:"
---

<objective>
Close 3 UAT gaps from phase 01: (1) register pyannote health checks in `meet doctor`, (2) add `--update` flag to `meet init`, and (3) reinstall the editable package so pyannote.audio is available.

Purpose: All 3 issues are wiring/registration bugs -- the implementation code exists but is not connected.
Output: All 3 UAT test cases (4, 5, 8) should pass after this plan.

Note: D-05, D-06, D-07 were already closed by plan 01-02. The --update flag gap addressed here is a UAT regression discovered after 01-02 execution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@meeting_notes/cli/commands/doctor.py
@meeting_notes/cli/commands/init.py
@meeting_notes/services/checks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register pyannote checks in doctor.py and add --update flag to init.py</name>
  <files>meeting_notes/cli/commands/doctor.py, meeting_notes/cli/commands/init.py</files>
  <action>
**IMPORTANT disambiguation:** init.py `_run_inline_doctor()` already has 3 `suite.register()` calls for PyannoteCheck, HuggingFaceTokenCheck, and PyannoteModelCheck (added in plan 01-02). Those are for the inline doctor run during `meet init`. This task is about **doctor.py** (the standalone `meet doctor` command), which is a SEPARATE file that has its OWN `HealthCheckSuite` and is MISSING the pyannote registrations. Only doctor.py needs changes for the check registrations.

**doctor.py -- 3 missing imports and 3 missing registrations (per D-13, D-14, D-15):**

1. Add `PyannoteCheck`, `HuggingFaceTokenCheck`, `PyannoteModelCheck` to the import block from `meeting_notes.services.checks` (line 10-22). These classes already exist in checks.py at lines 430, 454, 487.

2. Add 3 `suite.register()` calls after the existing `NotionDatabaseCheck` registration (after line 47):
   ```python
   suite.register(PyannoteCheck())
   suite.register(HuggingFaceTokenCheck(config.huggingface.token))
   suite.register(PyannoteModelCheck())
   ```
   Note: `HuggingFaceTokenCheck` takes `config.huggingface.token` as constructor arg (same pattern as `NotionTokenCheck(config.notion.token)`). The other two take no args.

3. Also need to import `HuggingFaceConfig` access -- `config` is already loaded from `Config.load(config_path)` which has `.huggingface.token`. No additional import needed since `Config` already includes `huggingface` field.

**init.py -- missing --update Click option (UAT regression from 01-02):**

1. Add `@click.option('--update', is_flag=True, default=False, help='Update specific config fields.')` decorator to the `init` command (between `@click.command()` and `@click.pass_context` on lines 243-244).

2. Add `update` parameter to `init()` function signature: `def init(ctx: click.Context, update: bool):`

3. Add early-exit guard at top of `init()` body (right after the `quiet` assignment on line 247):
   ```python
   if update:
       if not config_path.exists():
           console.print("[red]No config found. Run 'meet init' first.[/red]")
           raise SystemExit(1)
       config = Config.load(config_path)
       _update_specific_fields(config, config_path)
       _run_test_recording(config.audio.microphone_device_index)
       _run_inline_doctor(config)
       return
   ```
   This mirrors the existing (U)pdate path on lines 262-267 but is triggered by the CLI flag directly.
  </action>
  <verify>
    <automated>grep -q "suite.register(PyannoteCheck" meeting_notes/cli/commands/doctor.py && grep -q "suite.register(HuggingFaceTokenCheck" meeting_notes/cli/commands/doctor.py && grep -q "suite.register(PyannoteModelCheck" meeting_notes/cli/commands/doctor.py && echo "doctor.py: all 3 registrations present" && .venv/bin/python -m pytest tests/test_doctor_command.py tests/test_init.py tests/test_checks.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
  - `meet doctor` source code imports and registers PyannoteCheck, HuggingFaceTokenCheck, PyannoteModelCheck (3 imports + 3 suite.register calls in doctor.py)
  - `meet init --update` is a valid CLI invocation (Click accepts the flag)
  - All existing tests in test_doctor_command.py, test_init.py, and test_checks.py continue to pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Reinstall editable package to resolve stale pyannote.audio dependency</name>
  <files>pyproject.toml</files>
  <action>
The editable install currently points to a deleted worktree (.claude/worktrees/agent-a169d200) whose pyproject.toml only had click+rich. The main repo pyproject.toml correctly declares pyannote.audio==3.3.2. Fix:

1. Run from the project root: `.venv/bin/pip install -e .`
   This re-registers the editable install against the current pyproject.toml which includes pyannote.audio==3.3.2 and all other Phase 01 dependencies. No changes to pyproject.toml itself -- this task verifies the existing pyproject.toml is correctly installed.

2. Verify: `.venv/bin/python -c "import pyannote.audio; print('pyannote.audio imported successfully')"`

3. Verify the `meet` CLI entry point works: `.venv/bin/meet doctor` should now show PyannoteCheck, HuggingFaceTokenCheck, and PyannoteModelCheck in its output (these were registered in Task 1).

Note: This is a potentially slow operation (pyannote.audio has heavy dependencies including torch). Allow up to 10 minutes for pip install.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pyannote.audio; print('OK')" && .venv/bin/python -m pytest tests/test_checks.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>
  - `import pyannote.audio` succeeds in the project venv
  - `meet doctor` output includes pyannote-related health checks
  - All test_checks.py tests pass (PyannoteCheck, HuggingFaceTokenCheck, PyannoteModelCheck)
  </done>
</task>

</tasks>

<verification>
1. `grep -q "suite.register(PyannoteCheck" meeting_notes/cli/commands/doctor.py` -- exits 0
2. `grep -q "suite.register(HuggingFaceTokenCheck" meeting_notes/cli/commands/doctor.py` -- exits 0
3. `grep -q "suite.register(PyannoteModelCheck" meeting_notes/cli/commands/doctor.py` -- exits 0
4. `grep -q "\-\-update" meeting_notes/cli/commands/init.py` -- exits 0
5. `.venv/bin/python -c "import pyannote.audio"` -- no error
6. `.venv/bin/python -m pytest tests/test_doctor_command.py tests/test_init.py tests/test_checks.py -x -q` -- all pass
</verification>

<success_criteria>
- UAT Test 4 (meet init --update) is unblocked: the --update flag exists and routes to _update_specific_fields
- UAT Test 5 (meet doctor pyannote checks) is fixed: 3 new checks appear in doctor output
- UAT Test 8 (pyannote diarization) is unblocked: pyannote.audio is importable
- All existing tests continue to pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/01-srt-output-and-speaker-diarization-for-transcription-pipeline/01-04-SUMMARY.md`
</output>
