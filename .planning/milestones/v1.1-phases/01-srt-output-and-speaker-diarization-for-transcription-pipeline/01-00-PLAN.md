---
phase: 01-srt-output-and-speaker-diarization-for-transcription-pipeline
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_transcription.py
  - tests/test_transcribe_command.py
  - tests/test_checks.py
  - tests/test_config.py
  - tests/test_init.py
  - tests/test_summarize_command.py
autonomous: true
requirements:
  - NYQUIST-W0

must_haves:
  truths:
    - "All Wave 0 test stubs exist and are skipped (not failing)"
    - "pytest tests/ -x passes with all new stubs marked @pytest.mark.skip"
  artifacts:
    - path: "tests/test_transcription.py"
      provides: "SRT + diarization unit test stubs"
      contains: "test_srt_timestamp_format"
    - path: "tests/test_transcribe_command.py"
      provides: "SRT file creation + diarization integration test stubs"
      contains: "test_srt_file_created"
    - path: "tests/test_checks.py"
      provides: "Pyannote + HF health check test stubs"
      contains: "test_pyannote_check_error"
    - path: "tests/test_config.py"
      provides: "HF token config round-trip test stub"
      contains: "test_config_hf_token_roundtrip"
    - path: "tests/test_init.py"
      provides: "HF token init wizard test stubs"
      contains: "test_init_collects_hf_token"
    - path: "tests/test_summarize_command.py"
      provides: "Diarized transcript preference test stub"
      contains: "test_prefers_diarized_transcript"
  key_links: []
---

<objective>
Create all Nyquist Wave 0 test stubs so every subsequent plan has a pre-existing test to flip from skip to green.

Purpose: Satisfy the Nyquist validation contract. Every task in Plans 01-03 must have a corresponding test that existed BEFORE the implementation was written.
Output: 17 test stubs across 6 test files, all marked @pytest.mark.skip("Wave 0 stub"), full test suite still passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-srt-output-and-speaker-diarization-for-transcription-pipeline/01-CONTEXT.md
@.planning/phases/01-srt-output-and-speaker-diarization-for-transcription-pipeline/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test stubs to existing test files</name>
  <files>tests/test_transcription.py, tests/test_transcribe_command.py, tests/test_config.py, tests/test_init.py, tests/test_summarize_command.py</files>
  <read_first>tests/test_transcription.py, tests/test_transcribe_command.py, tests/test_config.py, tests/test_init.py, tests/test_summarize_command.py</read_first>
  <action>
Append the following test stubs to each file. Every stub MUST be decorated with `@pytest.mark.skip(reason="Wave 0 stub — implementation pending")` and have a docstring describing the expected behavior. Import pytest at top if not already imported.

**tests/test_transcription.py** — append after existing tests:

```python
@pytest.mark.skip(reason="Wave 0 stub — implementation pending")
def test_srt_timestamp_format():
    """seconds_to_srt_timestamp(3661.5) returns '01:01:01,500'."""
    from meeting_notes.services.transcription import seconds_to_srt_timestamp
    assert seconds_to_srt_timestamp(3661.5) == "01:01:01,500"

@pytest.mark.skip(reason="Wave 0 stub — implementation pending")
def test_generate_srt():
    """generate_srt() produces valid SRT with 1-based indices and HH:MM:SS,mmm timestamps."""
    from meeting_notes.services.transcription import generate_srt
    segments = [
        {"start": 0.0, "end": 2.5, "text": " Hello world"},
        {"start": 3.0, "end": 5.0, "text": " Goodbye"},
    ]
    srt = generate_srt(segments)
    assert "1\n00:00:00,000 --> 00:00:02,500\nHello world" in srt
    assert "2\n00:00:03,000 --> 00:00:05,000\nGoodbye" in srt

@pytest.mark.skip(reason="Wave 0 stub — implementation pending")
def test_generate_srt_with_speakers():
    """generate_srt() with speaker_map prefixes speaker tag on each entry."""
    from meeting_notes.services.transcription import generate_srt
    segments = [
        {"start": 0.0, "end": 2.0, "text": " Hello"},
        {"start": 2.0, "end": 4.0, "text": " Hi there"},
    ]
    speaker_map = {0: "SPEAKER_00", 1: "SPEAKER_01"}
    srt = generate_srt(segments, speaker_map=speaker_map)
    assert "SPEAKER_00: Hello" in srt
    assert "SPEAKER_01: Hi there" in srt

@pytest.mark.skip(reason="Wave 0 stub — implementation pending")
def test_transcribe_returns_segments(tmp_path, monkeypatch):
    """transcribe_audio() returns (text, segments) tuple instead of just text."""
    from meeting_notes.services import transcription as trans_module
    wav_file = tmp_path / "test.wav"
    wav_file.write_bytes(b"\x00" * 1000)
    fake_segments = [{"start": 0.0, "end": 2.0, "text": " Hello"}]
    monkeypatch.setattr(trans_module.mlx_whisper, "transcribe", lambda *a, **kw: {"text": "Hello", "segments": fake_segments})
    cfg = Config()
    text, segments = trans_module.transcribe_audio(wav_file, cfg)
    assert text == "Hello"
    assert segments == fake_segments

@pytest.mark.skip(reason="Wave 0 stub — implementation pending")
def test_speaker_segment_merge():
    """assign_speakers_to_segments() assigns speakers by max overlap."""
    from meeting_notes.services.transcription import assign_speakers_to_segments
    pass  # Full test in Plan 03

@pytest.mark.skip(reason="Wave 0 stub — implementation pending")
def test_diarized_txt_grouping():
    """build_diarized_txt() groups consecutive same-speaker segments."""
    from meeting_notes.services.transcription import build_diarized_txt
    pass  # Full test in Plan 03
```

**tests/test_transcribe_command.py** — append after existing tests:

```python
@pytest.mark.skip(reason="Wave 0 stub — implementation pending")
def test_srt_file_created(runner, tmp_path):
    """meet transcribe writes a .srt file alongside the .txt file."""
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation pending")
def test_metadata_includes_srt_fields(runner, tmp_path):
    """Metadata JSON includes srt_path and diarization_succeeded fields."""
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation pending")
def test_diarization_skips_without_hf_token(runner, tmp_path):
    """When HF token is missing, diarization is skipped with a yellow warning."""
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation pending")
def test_diarization_graceful_failure(runner, tmp_path):
    """When diarization raises an exception, transcription continues without speaker labels."""
    pass
```

**tests/test_config.py** — append after existing tests:

```python
@pytest.mark.skip(reason="Wave 0 stub — implementation pending")
def test_config_hf_token_roundtrip(tmp_path):
    """Config with huggingface.token saves and loads correctly."""
    pass
```

**tests/test_init.py** — append after existing tests:

```python
@pytest.mark.skip(reason="Wave 0 stub — implementation pending")
def test_init_collects_hf_token():
    """meet init wizard collects and saves HuggingFace token."""
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation pending")
def test_update_includes_hf_token():
    """_update_specific_fields includes HuggingFace token as a selectable field."""
    pass
```

**tests/test_summarize_command.py** — append after existing tests:

```python
@pytest.mark.skip(reason="Wave 0 stub — implementation pending")
def test_prefers_diarized_transcript(runner, tmp_path):
    """meet summarize uses diarized_transcript_path from metadata when available."""
    pass
```

IMPORTANT: Do NOT modify any existing test code. Only APPEND new stubs at the bottom of each file.
  </action>
  <verify>
    <automated>cd /Users/C9W7QTPQGK-juan.ramirez/Projects/meeting-notes-v0 && pytest tests/ -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_transcription.py contains `def test_srt_timestamp_format`
    - tests/test_transcription.py contains `def test_generate_srt`
    - tests/test_transcription.py contains `def test_generate_srt_with_speakers`
    - tests/test_transcription.py contains `def test_transcribe_returns_segments`
    - tests/test_transcription.py contains `def test_speaker_segment_merge`
    - tests/test_transcription.py contains `def test_diarized_txt_grouping`
    - tests/test_transcribe_command.py contains `def test_srt_file_created`
    - tests/test_transcribe_command.py contains `def test_metadata_includes_srt_fields`
    - tests/test_transcribe_command.py contains `def test_diarization_skips_without_hf_token`
    - tests/test_transcribe_command.py contains `def test_diarization_graceful_failure`
    - tests/test_checks.py contains `def test_pyannote_check_error`
    - tests/test_checks.py contains `def test_hf_token_check_warning_no_token`
    - tests/test_checks.py contains `def test_pyannote_model_check_warning`
    - tests/test_config.py contains `def test_config_hf_token_roundtrip`
    - tests/test_init.py contains `def test_init_collects_hf_token`
    - tests/test_init.py contains `def test_update_includes_hf_token`
    - tests/test_summarize_command.py contains `def test_prefers_diarized_transcript`
    - `pytest tests/ -x` exits 0 (all stubs skipped, existing tests pass)
  </acceptance_criteria>
  <done>17 test stubs exist across 6 files, all marked skip, full test suite green</done>
</task>

<task type="auto">
  <name>Task 2: Create new test_checks.py for pyannote health check stubs</name>
  <files>tests/test_checks.py</files>
  <read_first>tests/test_health_check.py, meeting_notes/core/health_check.py</read_first>
  <action>
Create a new file `tests/test_checks.py` with 3 test stubs for the pyannote-related health checks. This is a NEW file (no existing test_checks.py exists — the existing health check tests are in test_health_check.py and test_notion_checks.py).

```python
"""Unit tests for pyannote-related health checks (Phase 1)."""
import pytest

from meeting_notes.core.health_check import CheckResult, CheckStatus


@pytest.mark.skip(reason="Wave 0 stub — implementation pending")
def test_pyannote_check_error():
    """PyannoteCheck returns ERROR when pyannote.audio is not importable."""
    from meeting_notes.services.checks import PyannoteCheck
    # Will mock builtins.__import__ to raise ImportError for pyannote.audio
    pass


@pytest.mark.skip(reason="Wave 0 stub — implementation pending")
def test_hf_token_check_warning_no_token():
    """HuggingFaceTokenCheck returns WARNING when token is None."""
    from meeting_notes.services.checks import HuggingFaceTokenCheck
    pass


@pytest.mark.skip(reason="Wave 0 stub — implementation pending")
def test_pyannote_model_check_warning(tmp_path):
    """PyannoteModelCheck returns WARNING when model cache dir does not exist."""
    from meeting_notes.services.checks import PyannoteModelCheck
    pass
```

NOTE: The imports of PyannoteCheck, HuggingFaceTokenCheck, PyannoteModelCheck will fail at test collection time if the classes don't exist yet. Since the stubs are marked `@pytest.mark.skip`, pytest skips them BEFORE executing the body — but the import inside the function body won't run. This is correct. The import is inside the function body (not at module level) specifically so the file can be collected even before the classes exist.
  </action>
  <verify>
    <automated>cd /Users/C9W7QTPQGK-juan.ramirez/Projects/meeting-notes-v0 && pytest tests/test_checks.py -x -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_checks.py exists as a new file
    - tests/test_checks.py contains `def test_pyannote_check_error`
    - tests/test_checks.py contains `def test_hf_token_check_warning_no_token`
    - tests/test_checks.py contains `def test_pyannote_model_check_warning`
    - `pytest tests/test_checks.py -x` exits 0 with 3 skipped
  </acceptance_criteria>
  <done>3 health check test stubs exist in new test_checks.py, all skipped, pytest green</done>
</task>

</tasks>

<verification>
```bash
# All stubs exist and full suite passes
pytest tests/ -x
# Count skipped tests (should be 17)
pytest tests/ -v 2>&1 | grep -c "SKIPPED"
```
</verification>

<success_criteria>
- 17 test stubs across 6 files
- All stubs decorated with @pytest.mark.skip
- Full test suite passes (existing tests unaffected)
- Every test name matches the VALIDATION.md task IDs
</success_criteria>

<output>
After completion, create `.planning/phases/01-srt-output-and-speaker-diarization-for-transcription-pipeline/01-00-SUMMARY.md`
</output>
